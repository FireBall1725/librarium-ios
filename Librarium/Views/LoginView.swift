import SwiftUI

// LoginView is superseded by AddServerView in the multi-server architecture.
// Kept as an empty stub so any stale Xcode references compile cleanly.
struct LoginView: View {
    var body: some View { EmptyView() }
}
