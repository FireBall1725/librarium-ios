import SwiftUI

// ServerSettingsView is superseded by AccountsSettingsSheet in the multi-server architecture.
// Kept as an empty stub so any stale Xcode references compile cleanly.
struct ServerSettingsView: View {
    var body: some View { EmptyView() }
}
